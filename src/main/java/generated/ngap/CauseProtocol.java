package generated.ngap;

/**
 * This class file was automatically generated by yasn1c (ASN.1 Compiler).
 * DO NOT EDIT THIS FILE MANUALLY.
 *
 * Generated with command:
 * java -jar yasn1c.jar ../../src/test/resources/modules/NGAP_Full.asn -o D:\ngap -p generated.ngap -e APER,XER
 */

import javax.xml.stream.XMLStreamReader;
import javax.xml.stream.XMLStreamWriter;
import javax.xml.stream.XMLStreamConstants;
import javax.xml.stream.XMLStreamException;
import tr.com.cicerali.util.ClassNameUtil;
import tr.com.cicerali.aper.BitStreamReader;
import tr.com.cicerali.aper.BitStreamWriter;

public class CauseProtocol {

    public enum Value { 
        transfer_Syntax_Error("transfer-syntax-error"),
        abstract_Syntax_Error_Reject("abstract-syntax-error-reject"),
        abstract_Syntax_Error_Ignore_And_Notify("abstract-syntax-error-ignore-and-notify"),
        message_Not_Compatible_With_Receiver_State("message-not-compatible-with-receiver-state"),
        semantic_Error("semantic-error"),
        abstract_Syntax_Error_Falsely_Constructed_Message("abstract-syntax-error-falsely-constructed-message"),
        unspecified("unspecified"),
        UNKNOWN("UNKNOWN");

        private final String asn1Name;

        Value(String asn1Name) {
            this.asn1Name = asn1Name;
        }

        public String getAsn1Name() {
            return asn1Name;
        }

        public static Value fromAsn1Name(String asn1Name) {
            for (Value v : values()) {
                if (v.asn1Name.equals(asn1Name)) {
                    return v;
                }
            }
            return UNKNOWN;
        }
    }

    private Value value;
    public Value getValue() { return value; }
    public void setValue(Value value) {
        if (value == Value.UNKNOWN) {
            throw new IllegalArgumentException("Cannot set value to UNKNOWN. This value is read-only for forward compatibility.");
        }
        this.value = value;
    }
    private void setInternalValue(Value value) { this.value = value; }
    @Override
    public String toString() { return String.valueOf(value); }

    public void encodeXER(XMLStreamWriter writer, String name) throws XMLStreamException {
        if (value == Value.UNKNOWN) {
            throw new RuntimeException("Cannot encode UNKNOWN value for ENUMERATED type CauseProtocol");
        }
        writer.writeStartElement(name);
        writer.writeEmptyElement(value.getAsn1Name());
        writer.writeEndElement();
    }

    public void encodeXER(XMLStreamWriter writer) throws XMLStreamException {
        encodeXER(writer, "CauseProtocol");
    }

    public static CauseProtocol decodeXER(XMLStreamReader reader, String name) throws XMLStreamException {
        CauseProtocol instance = new CauseProtocol();
        while (reader.hasNext() && (!reader.isStartElement() || !reader.getLocalName().equals(name))) { reader.next(); }
        if (reader.hasNext()) {
            reader.nextTag();
            String text = reader.getLocalName();
            instance.setValue(Value.fromAsn1Name(text));
        }
        return instance;
    }

    public static CauseProtocol decodeXER(XMLStreamReader reader) throws XMLStreamException {
        return decodeXER(reader, "CauseProtocol");
    }

    public void encodeAPER(BitStreamWriter writer) {
        if (value == Value.UNKNOWN) {
            throw new RuntimeException("Cannot encode UNKNOWN value for ENUMERATED type CauseProtocol");
        }
        if (value.ordinal() < 7) {
            writer.writeBit(false); // No extension
            writer.writeInteger(value.ordinal(), 0L, 6L);
        } else {
            writer.writeBit(true); // Extension present
            // Extension values are encoded as normally small non-negative integers
            // The value is the index into the extension list (0-based)
            writer.writeNormallySmallNonNegativeWholeNumber(value.ordinal() - 7);
        }
    }

    public static CauseProtocol decodeAPER(BitStreamReader reader) {
        CauseProtocol instance = new CauseProtocol();
        if (reader.readBit()) { // Extension present
             // Extension values are encoded as normally small non-negative integers
             // The value is the index into the extension list (0-based)
             int extIndex = (int) reader.readNormallySmallNonNegativeWholeNumber();
             // Map extension index back to enum value
             // The extension items are appended to the main items list in the model
             // So the index in Value.values() is (root items size) + extIndex
             int enumIndex = 7 + extIndex;
             if (enumIndex >= Value.values().length) {
                 // Unknown extension value, treat as UNKNOWN
                 instance.setInternalValue(Value.UNKNOWN);
             } else {
                 instance.setValue(Value.values()[enumIndex]);
             }
        } else {
             instance.setValue(Value.values()[(int)reader.readInteger(0L, 6L)]);
        }
        return instance;
    }

}
