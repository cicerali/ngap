package generated.ngap;

/**
 * This class file was automatically generated by yasn1c (ASN.1 Compiler).
 * DO NOT EDIT THIS FILE MANUALLY.
 *
 * Generated with command:
 * java -jar yasn1c.jar ../../src/test/resources/modules/NGAP_Full.asn -o D:\ngap -p generated.ngap -e APER,XER
 */

import javax.xml.stream.XMLStreamReader;
import javax.xml.stream.XMLStreamWriter;
import javax.xml.stream.XMLStreamConstants;
import javax.xml.stream.XMLStreamException;
import tr.com.cicerali.util.ClassNameUtil;
import tr.com.cicerali.aper.BitStreamReader;
import tr.com.cicerali.aper.BitStreamWriter;

public class NgENB_ID {

    public enum Choice { MACRONGENB_ID, SHORTMACRONGENB_ID, LONGMACRONGENB_ID, CHOICE_EXTENSIONS }

    private Choice choice;
    private Object value;

    public Choice getChoice() { return choice; }
    public Object getValue() { return value; }

    public void setMacroNgENB_ID(tr.com.cicerali.aper.BitString value) {
        this.choice = Choice.MACRONGENB_ID;
        this.value = value;
    }
    public tr.com.cicerali.aper.BitString getMacroNgENB_ID() {
        if (choice == Choice.MACRONGENB_ID) return (tr.com.cicerali.aper.BitString) value;
        return null;
    }

    public void setShortMacroNgENB_ID(tr.com.cicerali.aper.BitString value) {
        this.choice = Choice.SHORTMACRONGENB_ID;
        this.value = value;
    }
    public tr.com.cicerali.aper.BitString getShortMacroNgENB_ID() {
        if (choice == Choice.SHORTMACRONGENB_ID) return (tr.com.cicerali.aper.BitString) value;
        return null;
    }

    public void setLongMacroNgENB_ID(tr.com.cicerali.aper.BitString value) {
        this.choice = Choice.LONGMACRONGENB_ID;
        this.value = value;
    }
    public tr.com.cicerali.aper.BitString getLongMacroNgENB_ID() {
        if (choice == Choice.LONGMACRONGENB_ID) return (tr.com.cicerali.aper.BitString) value;
        return null;
    }

    public void setChoice_Extensions(ProtocolIE_SingleContainer value) {
        this.choice = Choice.CHOICE_EXTENSIONS;
        this.value = value;
    }
    public ProtocolIE_SingleContainer getChoice_Extensions() {
        if (choice == Choice.CHOICE_EXTENSIONS) return (ProtocolIE_SingleContainer) value;
        return null;
    }

    public void encodeXER(XMLStreamWriter writer, String name) throws XMLStreamException {
        writer.writeStartElement(name);
        if (this.getChoice() != null) {
            switch (this.getChoice()) {
                case MACRONGENB_ID:
                    writer.writeStartElement("macroNgENB-ID");
                    {
                        tr.com.cicerali.aper.BitString bs = this.getMacroNgENB_ID();
                        int len = bs.getLength();
                        int minSizeVal = 20;
                        int targetLen;
                        if (minSizeVal > 0 && minSizeVal % 4 == 0) {
                            targetLen = minSizeVal;
                        } else {
                            targetLen = ((4 - (len % 4)) % 4) + len;
                        }
                        if (targetLen < len) targetLen = len;
                        StringBuilder sb = new StringBuilder();
                        for (int i = targetLen - 1; i >= 0; i--) {
                            sb.append(bs.getValue().get(i) ? "1" : "0");
                        }
                        writer.writeCharacters(sb.toString());
                    }
                    writer.writeEndElement();
                    break;
                case SHORTMACRONGENB_ID:
                    writer.writeStartElement("shortMacroNgENB-ID");
                    {
                        tr.com.cicerali.aper.BitString bs = this.getShortMacroNgENB_ID();
                        int len = bs.getLength();
                        int minSizeVal = 18;
                        int targetLen;
                        if (minSizeVal > 0 && minSizeVal % 4 == 0) {
                            targetLen = minSizeVal;
                        } else {
                            targetLen = ((4 - (len % 4)) % 4) + len;
                        }
                        if (targetLen < len) targetLen = len;
                        StringBuilder sb = new StringBuilder();
                        for (int i = targetLen - 1; i >= 0; i--) {
                            sb.append(bs.getValue().get(i) ? "1" : "0");
                        }
                        writer.writeCharacters(sb.toString());
                    }
                    writer.writeEndElement();
                    break;
                case LONGMACRONGENB_ID:
                    writer.writeStartElement("longMacroNgENB-ID");
                    {
                        tr.com.cicerali.aper.BitString bs = this.getLongMacroNgENB_ID();
                        int len = bs.getLength();
                        int minSizeVal = 21;
                        int targetLen;
                        if (minSizeVal > 0 && minSizeVal % 4 == 0) {
                            targetLen = minSizeVal;
                        } else {
                            targetLen = ((4 - (len % 4)) % 4) + len;
                        }
                        if (targetLen < len) targetLen = len;
                        StringBuilder sb = new StringBuilder();
                        for (int i = targetLen - 1; i >= 0; i--) {
                            sb.append(bs.getValue().get(i) ? "1" : "0");
                        }
                        writer.writeCharacters(sb.toString());
                    }
                    writer.writeEndElement();
                    break;
                case CHOICE_EXTENSIONS:
                    this.getChoice_Extensions().encodeXER(writer, "choice-Extensions");
                    break;
            }
        }
        writer.writeEndElement();
    }

    public void encodeXER(XMLStreamWriter writer) throws XMLStreamException {
        encodeXER(writer, "NgENB-ID");
    }

    public static NgENB_ID decodeXER(XMLStreamReader reader, String name) throws XMLStreamException {
        NgENB_ID instance = new NgENB_ID();
        while (reader.hasNext() && (!reader.isStartElement() || !reader.getLocalName().equals(name))) { reader.next(); }
        while (reader.hasNext()) {
            int event = reader.nextTag();
            if (event == XMLStreamConstants.START_ELEMENT) {
                String localName = reader.getLocalName();
                switch (localName) {
                    case "macroNgENB-ID":
                        {
                            String binStr = reader.getElementText();
                            java.util.BitSet bs = new java.util.BitSet(binStr.length());
                            for (int i = 0; i < binStr.length(); i++) {
                                if (binStr.charAt(i) == '1') bs.set(binStr.length() - 1 - i);
                            }
                            instance.setMacroNgENB_ID(new tr.com.cicerali.aper.BitString(bs, binStr.length()));
                        }
                        break;
                    case "shortMacroNgENB-ID":
                        {
                            String binStr = reader.getElementText();
                            java.util.BitSet bs = new java.util.BitSet(binStr.length());
                            for (int i = 0; i < binStr.length(); i++) {
                                if (binStr.charAt(i) == '1') bs.set(binStr.length() - 1 - i);
                            }
                            instance.setShortMacroNgENB_ID(new tr.com.cicerali.aper.BitString(bs, binStr.length()));
                        }
                        break;
                    case "longMacroNgENB-ID":
                        {
                            String binStr = reader.getElementText();
                            java.util.BitSet bs = new java.util.BitSet(binStr.length());
                            for (int i = 0; i < binStr.length(); i++) {
                                if (binStr.charAt(i) == '1') bs.set(binStr.length() - 1 - i);
                            }
                            instance.setLongMacroNgENB_ID(new tr.com.cicerali.aper.BitString(bs, binStr.length()));
                        }
                        break;
                    case "choice-Extensions":
                        instance.setChoice_Extensions(ProtocolIE_SingleContainer.decodeXER(reader, "choice-Extensions"));
                        break;
                    default: reader.next(); break;
                }
            } else if (event == XMLStreamConstants.END_ELEMENT && reader.getLocalName().equals(name)) {
                break;
            }
        }
        return instance;
    }

    public static NgENB_ID decodeXER(XMLStreamReader reader) throws XMLStreamException {
        return decodeXER(reader, "NgENB-ID");
    }

    public void encodeAPER(BitStreamWriter writer) {
        if (this.getChoice() != null) {
            switch (this.getChoice()) {
                case MACRONGENB_ID:
                    writer.writeInteger(0, 0L, 3L);
                    writer.writeBitString(this.getMacroNgENB_ID().getValue(), this.getMacroNgENB_ID().getLength(), 20, 20, false);
                    break;
                case SHORTMACRONGENB_ID:
                    writer.writeInteger(1, 0L, 3L);
                    writer.writeBitString(this.getShortMacroNgENB_ID().getValue(), this.getShortMacroNgENB_ID().getLength(), 18, 18, false);
                    break;
                case LONGMACRONGENB_ID:
                    writer.writeInteger(2, 0L, 3L);
                    writer.writeBitString(this.getLongMacroNgENB_ID().getValue(), this.getLongMacroNgENB_ID().getLength(), 21, 21, false);
                    break;
                case CHOICE_EXTENSIONS:
                    writer.writeInteger(3, 0L, 3L);
                    this.getChoice_Extensions().encodeAPER(writer);
                    break;
            }
        }
    }

    public static NgENB_ID decodeAPER(BitStreamReader reader) {
        NgENB_ID instance = new NgENB_ID();
            int choiceIndex = (int) reader.readInteger(0L, 3L);
            switch (choiceIndex) {
                case 0:
                    instance.setMacroNgENB_ID(new tr.com.cicerali.aper.BitString(reader.readBitString(20, 20, false), reader.getLastReadBitStringLength()));
                    break;
                case 1:
                    instance.setShortMacroNgENB_ID(new tr.com.cicerali.aper.BitString(reader.readBitString(18, 18, false), reader.getLastReadBitStringLength()));
                    break;
                case 2:
                    instance.setLongMacroNgENB_ID(new tr.com.cicerali.aper.BitString(reader.readBitString(21, 21, false), reader.getLastReadBitStringLength()));
                    break;
                case 3:
                    instance.setChoice_Extensions(ProtocolIE_SingleContainer.decodeAPER(reader));
                    break;
            }
        return instance;
    }

}
