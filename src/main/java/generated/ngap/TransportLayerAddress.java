package generated.ngap;

/**
 * This class file was automatically generated by yasn1c (ASN.1 Compiler).
 * DO NOT EDIT THIS FILE MANUALLY.
 *
 * Generated with command:
 * java -jar yasn1c.jar ../../src/test/resources/modules/NGAP_Full.asn -o D:\ngap -p generated.ngap -e APER,XER
 */

import javax.xml.stream.XMLStreamReader;
import javax.xml.stream.XMLStreamWriter;
import javax.xml.stream.XMLStreamConstants;
import javax.xml.stream.XMLStreamException;
import tr.com.cicerali.util.ClassNameUtil;
import tr.com.cicerali.aper.BitStreamReader;
import tr.com.cicerali.aper.BitStreamWriter;
import java.util.BitSet;

public class TransportLayerAddress {

    private tr.com.cicerali.aper.BitString value;
    public tr.com.cicerali.aper.BitString getValue() { return value; }
    public void setValue(tr.com.cicerali.aper.BitString value) { this.value = value; }
    @Override
    public String toString() { return String.valueOf(value); }

    public void encodeXER(XMLStreamWriter writer, String name) throws XMLStreamException {
        writer.writeStartElement(name);
        // Convert BitSet to binary string (Big-Endian, padded to 4 bits)
        int len = value.getLength();
        int minSizeVal = 1;
        int targetLen;
        if (minSizeVal > 0 && minSizeVal % 4 == 0) {
            targetLen = minSizeVal;
        } else {
            targetLen = ((4 - (len % 4)) % 4) + len;
        }
        if (targetLen < len) targetLen = len;
        StringBuilder sb = new StringBuilder();
        for (int i = targetLen - 1; i >= 0; i--) {
            sb.append(value.getValue().get(i) ? "1" : "0");
        }
        writer.writeCharacters(sb.toString());
        writer.writeEndElement();
    }

    public void encodeXER(XMLStreamWriter writer) throws XMLStreamException {
        encodeXER(writer, "TransportLayerAddress");
    }

    public static TransportLayerAddress decodeXER(XMLStreamReader reader, String name) throws XMLStreamException {
        TransportLayerAddress instance = new TransportLayerAddress();
        while (reader.hasNext() && (!reader.isStartElement() || !reader.getLocalName().equals(name))) { reader.next(); }
        String binStr = reader.getElementText();
        BitSet bs = new BitSet(binStr.length());
        for (int i = 0; i < binStr.length(); i++) {
            if (binStr.charAt(i) == '1') bs.set(binStr.length() - 1 - i);
        }
        instance.setValue(new tr.com.cicerali.aper.BitString(bs, binStr.length()));
        return instance;
    }

    public static TransportLayerAddress decodeXER(XMLStreamReader reader) throws XMLStreamException {
        return decodeXER(reader, "TransportLayerAddress");
    }

    public void encodeAPER(BitStreamWriter writer) {
        writer.writeBitString(value.getValue(), value.getLength(), 1, 160, true);
    }

    public static TransportLayerAddress decodeAPER(BitStreamReader reader) {
        TransportLayerAddress instance = new TransportLayerAddress();
        instance.setValue(new tr.com.cicerali.aper.BitString(reader.readBitString(1, 160, true), reader.getLastReadBitStringLength()));
        return instance;
    }

}
